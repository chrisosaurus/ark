features:
	ui cursor
	ui mouse cursor placement
	ui mouse select

technical debt:
	behavior/display of tabs (insert a character between/before them, have a tab within a line, shouldnt actually be 4 spaces but 4%n)

consider:

