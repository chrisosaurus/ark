features:
	ui mouse cursor placement
	ui scrolling
	ui mouse select
	complex cursor movement (proper moving down of lines, jump by word, etc. )

technical debt:
	drawing loop, char by char
	offsets within line vs vertical offsets, causes issues with mouse cursor and keyboard movement

consider:

