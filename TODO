features:
	ui scrolling
	ui mouse select
	complex cursor movement (proper moving down of lines, jump by word, etc. )

technical debt:
	drawing loop, char by char
	config.h included in llist.c, requires ark.h and ui.h :(

consider:

