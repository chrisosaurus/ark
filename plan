llist becomes a super type

make operator functions 'on' it that take and return `void *`

move line and buffer stuff into another file

---

create a span

begin work on styling (draw loop)
-consider moving the draw stuff out of ui (make ui more generic, it could dispatch to an external routine?


